# review3.0 修复记录

- 刷新令牌轮换改为带 token hash、用户、未撤销和未过期条件的原子 `updateMany`，并要求 `count === 1`，防止并发重放。
- 菜品删除检查更新数量，不存在、已删除或跨厨房资源统一返回 404。
- 菜品空 PATCH 被拒绝，避免无意义数据库写入。
- 新增空 PATCH 回归测试；API 类型检查、ESLint 和 11 项测试通过。

评审中要求的微信登录、菜单、库存、AI、上传、故事、情书、审计和限流等主体模块仍未完成，当前状态仍应视为阶段性实现。
