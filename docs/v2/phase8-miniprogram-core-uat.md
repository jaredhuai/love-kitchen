# Phase 8：小程序核心流程与合规 UAT

更新日期：2026-07-20。代码与自动化验收完成；真机预览涉及把仓库内小程序代码上传到已登录的微信开发者账号，当前尚未获得明确外发授权，故本任务保持 `IN_PROGRESS`。

## 已完成实现

- 登录：用户同意协议后调用微信隐私授权与 `wx.login`，将一次性 code 交给服务端；dev-login 只在 development 显示且 production 请求层再次阻断。
- 合规：隐私政策、用户协议、本人数据导出、当前设备退出、账号注销、冷静期提示和取消入口均可达。
- 隔离：登录保存 User/Kitchen/Membership；退出、刷新失败和注销均清除 Access/Refresh Token 与全部账号级 Store，设备标识和待取消注销凭证不跨账号展示业务数据。
- 流程：菜品、菜单、AI、“我们的”、库存、购物和情书均连接真实 API；新增页面提供 loading、empty、error 和 retry 状态；修正 Tab 导航和情书页面职责。

## 自动化证据

- 小程序测试：18/18（环境 6、请求 3、路由 2、合规/页面 6、跨账号清理 1）。
- TypeScript typecheck/build 与 `git diff --check` 通过。

## 真机签署清单

使用测试厨房和两个测试微信账号逐项记录设备、微信版本、时间、结果与截图：

1. 首次进入未勾选协议时不能登录；打开协议/隐私政策后返回状态正确；拒绝微信隐私授权有明确提示。
2. 两个账号分别登录，确认 code 不能重用；退出 A 后登录 B，不出现 A 的用户、厨房、通知、Feature Flag 或内容。
3. 菜品列表/详情/新增/编辑、今日菜单加入与移除、AI 成功/限额/超时、时间轴与成就均验证 loading、空、失败重试和成功态。
4. 库存添加/消耗、购物添加/勾选、情书未解锁/已解锁分别验证，跨厨房资源必须失败关闭。
5. 数据导出只包含本人授权范围；申请注销展示冷静期并退出全部会话；重新登录后取消注销成功。
6. 主包 Tab 与分包页面全部可达；返回栈、下拉刷新、弱网和中断请求无白屏或重复写入。

## 回滚

本次没有数据库变更。可回滚小程序版本到任务 9 提交；服务端 v1 路径与账号 Job 保持兼容。注销永久清除开关仍关闭。
