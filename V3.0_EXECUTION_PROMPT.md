# Love Kitchen 3.0 可执行 Prompt

你正在维护 `love-kitchen` 微信小程序、NestJS API 和 PostgreSQL/Prisma 数据库。请完整实现以下 3.0 需求。不要只修改界面或使用假数据；前端、API、数据库迁移、历史数据兼容、测试和部署文档必须同步完成。

## 一、执行原则

1. 开始前检查 `git status`、现有 Prisma schema、API 契约、小程序页面和测试。
2. 保留用户已有的本地配置、真实 AppID、生产 API 地址和未提交文件，不覆盖无关改动。
3. 禁止清空生产数据库。所有 schema 调整必须提供可重复执行的 Prisma migration。
4. 迁移前提供数据库备份命令；迁移必须兼容现有菜品、评价、餐单和上传记录。
5. 页面继续使用当前暖粉、奶油白、暖棕手绘风格，避免引入不一致的组件库。
6. 所有数量、评分、开业天数均来自真实数据，不得硬编码演示值。
7. 分阶段实现并在每阶段运行类型检查、单元测试及相关端到端测试。

## 二、需求清单

### 1. 真实菜品评分

- 删除未评价菜品默认显示的 `4.8（328）`。
- 评价数量为 `0` 时显示“暂无评价”，不显示星级数值或虚构数量。
- 有评价时展示真实平均分和真实评价数量，例如 `4.7（3）`。
- 平均分由真实评价计算，统一保留一位小数。
- 菜品列表、详情页、评价页使用同一套评分数据和展示规则。

验收：

- 新建但未评价的菜品显示“暂无评价”。
- 添加、修改或删除评价后，平均分与数量立即正确更新。

### 2. 添加/编辑菜品表单

- 表单标题栏中的“取消”按钮移动到最右侧并靠边对齐。
- 增加独立的“备注详情”字段，不与菜品简介、食材、步骤混用。
- 图片由单图改成多图：
  - 支持一次或分批选择多张图片；
  - 支持上传进度；
  - 支持预览、删除和调整顺序；
  - 第一张默认为封面，也允许用户选择其他图片作为封面；
  - 编辑菜品时正确回显现有图片；
  - 菜品详情页按顺序展示全部图片。
- 上传失败不能丢失已经成功上传的图片，也不能提交半成品引用。

建议数据结构：

- 新增 `Dish.notes`，允许为空。
- 新增 `DishImage`：`id`、`dishId`、`uploadId`、`sortOrder`、`isCover`、`createdAt`。
- 保留旧 `coverImageUrl` 作为兼容读取字段；迁移时将已有封面转换成第一张 `DishImage`，确认稳定后再决定是否删除旧字段。

### 3. 永久菜品与临时菜品

- “添加菜品”按钮改成下拉菜单，包含：
  - “添加永久菜品”
  - “添加临时菜品”
- 永久菜品进入长期菜单，可以反复安排餐次，保存固定食材和步骤。
- 临时菜品用于火锅等当天临时安排：
  - 食材和步骤可以仅针对当日填写；
  - 必须绑定具体日期或具体餐次；
  - 不出现在永久菜品库；
  - 用餐结束后保留在历史记录中；
  - 历史记录必须保留当时的菜名、图片、备注、食材和步骤快照，不能因后续数据修改而变化。

建议数据结构：

- `Dish.kind`: `PERMANENT | TEMPORARY`。
- 临时菜品包含 `effectiveDate`，并与 `MealPlan` 或 `MealLog` 建立明确关系。
- 历史记录使用快照字段或独立快照表，不只依赖当前 `Dish` 记录。

### 4. 九个菜品分类

最终分类以以下九类为准，之前“六分类”需求作废：

| 稳定代码 | 中文名称 |
| --- | --- |
| `MEAT` | 荤菜 |
| `VEGETABLE` | 素菜 |
| `SOUP_PORRIDGE` | 汤羹粥 |
| `DESSERT_SNACK` | 甜品零食 |
| `WESTERN` | 西餐 |
| `SEAFOOD` | 海鲜 |
| `DRINK` | 饮品 |
| `STAPLE` | 主食 |
| `OTHER` | 其他 |

- API 和数据库保存稳定代码，小程序显示中文名称。
- DTO 必须拒绝九类以外的新值。
- 为历史分类提供迁移映射：
  - `汤羹`、`粥` → `SOUP_PORRIDGE`
  - `甜品`、`零食`、`烘焙`、`小吃` → `DESSERT_SNACK`
  - `西餐` → `WESTERN`
  - `海鲜` → `SEAFOOD`
  - `饮品` → `DRINK`
  - `主食` → `STAPLE`
  - 可以可靠判断的荤菜、素菜分别迁移；
  - 无法可靠判断的旧分类迁移到 `OTHER`，不得根据菜名擅自猜测。
- 管理页面允许用户重新修改迁移到“其他”的菜品分类。

### 5. “所有菜单”九宫格

- “所有菜单”页面不再默认直接展示两列菜品卡片。
- 页面顶部固定显示 `3 × 3` 九宫格，顺序与上表一致。
- 每个模块展示：
  - 对应分类图标；
  - 中文分类名称；
  - 当前菜品数量；
  - 空分类显示“暂无菜品”，模块仍然保留。
- 点击模块后展示该分类菜品；提供清晰的返回/收起入口。
- 分类内菜品仍可进入详情和加入餐次。
- 临时菜品不混入永久分类菜单。

九张图标资源使用项目目录：

```text
apps/miniprogram/miniprogram/assets/category-icons/meat.png
apps/miniprogram/miniprogram/assets/category-icons/vegetable.png
apps/miniprogram/miniprogram/assets/category-icons/soup.png
apps/miniprogram/miniprogram/assets/category-icons/dessert.png
apps/miniprogram/miniprogram/assets/category-icons/western.png
apps/miniprogram/miniprogram/assets/category-icons/seafood.png
apps/miniprogram/miniprogram/assets/category-icons/drink.png
apps/miniprogram/miniprogram/assets/category-icons/staple.png
apps/miniprogram/miniprogram/assets/category-icons/other.png
```

图标文字必须由 WXML 渲染，不把生成图片中的文字作为分类名称。

### 6. 登录页开业天数

- 在爱心登录入口上方增加醒目提示：

```text
今天是小厨房开业第 X 天
```

- 开业基准日固定为 `2026-08-18`：
  - `2026-08-18` 当天为第 `1` 天；
  - `2026-08-18` 之前统一显示第 `0` 天；
  - 此后每天按自然日增加 `1`；
  - 按用户本地自然日计算；
  - 不使用固定数字；
- 不依赖数据库 `createdAt`，也不新增公开接口。
- 提示样式使用浅粉渐变、暖棕文字和轻微呼吸光效，不遮挡爱心登录按钮。

### 7. 菜品管理列表自动折叠

- “我们的 → 菜单菜品管理”默认最多显示前 `5` 个菜品。
- 超过 `5` 个时显示“展开其余 N 道菜”。
- 展开后显示全部，并提供“收起菜品”。
- 新增、编辑或删除菜品后，列表数量和折叠文案立即刷新。
- 折叠只影响显示，不改变 API 分页、排序或数据库数据。

## 三、API 与安全要求

1. 所有厨房数据接口继续使用 `KitchenAccessGuard`，严禁跨厨房访问。
2. 多图上传继续使用现有安全上传链路，校验文件类型、大小、归属厨房和访问权限。
3. 删除菜品时按现有软删除策略处理图片关系；不得误删其他菜品引用的上传文件。
4. 临时菜品创建、安排餐次和写入历史必须放在事务中，避免产生孤立记录。

## 四、实施顺序

1. 审计现有 schema、Dish API、上传模块、餐单、历史和小程序页面。
2. 编写 Prisma migration 和数据迁移规则。
3. 更新 Prisma Client、DTO、服务、仓储、API 契约和测试。
4. 更新添加/编辑菜品表单及多图组件。
5. 更新九分类选择、九宫格和分类菜品列表。
6. 更新真实评分展示。
7. 更新永久/临时菜品及历史快照。
8. 更新登录页开业天数。
9. 更新菜品管理五条折叠。
10. 完整验证后再提交，不得将 `.env.production`、真实 AppID、密钥或本地生成的无关 JS 文件提交。

## 五、最低测试要求

- API 和小程序 TypeScript 类型检查通过。
- Prisma schema validate 和 generate 通过。
- migration 在空数据库与包含旧分类、旧封面图的数据库上均可执行。
- 未评价、单条评价、多条评价的平均分测试。
- 九分类 DTO 校验和旧分类迁移测试。
- 永久/临时菜品生命周期测试。
- 多图新增、排序、封面切换、删除和越权测试。
- 开业日前、开业当天和跨日后的天数测试。
- 菜品数量为 `0`、`5`、`6` 时的折叠逻辑测试。
- 微信开发者工具中人工验证九宫格、图片预览、登录页和小屏适配。

## 六、交付标准

完成后必须提供：

1. 修改文件清单。
2. 数据库 migration 名称及迁移规则说明。
3. 测试命令和结果。
4. 对现有生产数据的影响说明。
5. 服务器备份、拉取、构建、迁移、健康检查和回滚命令。
6. 小程序重新编译、体验版上传和真机验证步骤。

不要在存在失败 migration、类型错误、测试失败或未处理历史数据映射时宣称 3.0 已完成。
